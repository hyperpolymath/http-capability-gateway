dsl_version: "1"
governance:
  global_verbs:
    - GET
  routes:
    - path: "^/health$"
      verbs: [GET]
    - path: "^/api/.*$"
      verbs: [GET, POST]
stealth:
  enabled: true
  status_code: 404
